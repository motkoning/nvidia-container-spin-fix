# Test Summary - Week 1, V. 1.2.0

## What Was Tested

- Syntax gate: both shipped scripts parsed by the Windows PowerShell 5.1 parser (5.1.26100.8972)
- Read-only smoke runs: CLI Diagnose, GUI `-SelfTest`
- Discrimination check: raw per-PID CIM CPU-time samples asserted non-null (guards against the fabricated-zero failure mode caught in code-review round 1)
- Worker precondition path, unelevated: `-Worker Fix` on a fixed-state machine
- **Elevated exit-code transport, end-to-end (post-release, this machine)**: `Start-Process -Verb RunAs -PassThru` → hidden elevated worker → `WaitForExit()` + `ExitCode` readout across the UAC integrity boundary
- **Live GUI pass (post-release, performed by the owner)**: launched via `START-HERE.bat`; consent dialog and states eyeballed — reported fine
- README verification snippet: executed verbatim (both the measuring branch and the no-process branch)

## Test Results

- Syntax: clean (both scripts)
- Diagnose: real samples (0.2–0.3% on the session instance), verdict "fix applied and working" — correct for this machine's state
- `-SelfTest`: `SelfTest OK: GUI constructed. PluginState=fixed`
- Discrimination check: non-null Kernel/User times for both container PIDs
- Worker precondition (unelevated): exit code 2 (already fixed), no file touched — matches the matrix
- **Elevated transport: exit code 2 received intact through the RunAs boundary, no file touched**
- Live GUI pass: OK per owner
- Coverage: no automated suite (by design, ARCHI.md §18)

## Key Findings

- The elevated exit-code transport — the one deferred item that was practically testable on this machine — works end-to-end. Tested mutation-free by exploiting the fixed state: the worker exits 2 before any file operation.
- Code-review round 1's Critical (silently fabricated 0% measurement) is the reason the discrimination check exists; a plain "0%" smoke result is not evidence of a working measurement.

## Notes

Remaining deferred verifications and their dispositions:

- **Real Fix/Revert renames**: closed by equivalence — the rename mechanism is byte-for-byte the same operation as v1.1.0, which was verified on this exact machine (RTX 5070 Ti) during the original investigation (evidence.md). Re-verifying live would re-enable the bug and cost two reboots (one experiment per reboot, ARCHI.md §10) for no new information.
- **Cross-account UAC (standard user elevating as a different admin)**: requires a second account; the exit-code design removes the failure mode by construction (no cross-account file paths). Deferred until a natural opportunity.
- **Conflict state in vivo**: fabricating files in DriverStore is out of bounds; logic verified by review on both surfaces plus the worker.
- **Localized Windows**: no localized system available; no locale-sensitive identifiers remain by construction (CIM classes + SIDs only).
